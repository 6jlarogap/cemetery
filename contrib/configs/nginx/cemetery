server {
    listen      80;
    server_name 127.0.0.1;
    client_max_body_size 100m;
    fastcgi_read_timeout 36000;
    ##access_log  /home/django/nginx_logs/nginx_access.log;
    ##error_log   /home/django/nginx_logs/nginx_error.log;

    # связь с fastcgi сервером
    location / {
        fastcgi_pass    unix:/var/run/django/youmemory.socket;
        include         fastcgi_params;

        fastcgi_param              PATH_INFO        $fastcgi_script_name;
        fastcgi_pass_header        Authorization;
        fastcgi_intercept_errors   off;
    }

    # статическое содержимое проекта
    location /media/ {
        alias /home/django/projects/youmemory/media/;
        expires 30d;
    }

    # статическое содержимое админки    
    location /admin_media/ {
        alias /home/django/projects/youmemory/django/contrib/admin/media/;
        expires 30d;
    }
}

